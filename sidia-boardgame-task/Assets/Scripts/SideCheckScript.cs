using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SideCheckScript : MonoBehaviour
{
    //This class exists so the DiceCheckZoneScript are able to know which dice is which (all sides from dice 1 have ID = 1)
    public int id;
}
